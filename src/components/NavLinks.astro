---
const {
  activePage,
  links,
  isMobileLink = false,
} = Astro.props as {
  activePage: string;
  links: { text: string; href: string }[];
  isMobileLink?: boolean;
};
---

{
  links?.map((link) => {
    const classNames = [
      isMobileLink ? "close-menu" : "",
      activePage === link.text.toLowerCase()
        ? "text-coral-pink! dark:text-peach!"
        : "",
    ]
      .join(" ")
      .trim();
    return (
      <a class={classNames} href={link.href}>
        {link.text}
      </a>
    );
  })
}
